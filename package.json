{
  "name": "dbd-bounty-hunter",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "make all && astro dev",
    "build": "make re && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:perks": "node scripts/check-perks.cjs",
    "check:characters": "node scripts/check-characters.cjs",
    "check:data": "npm run check:perks && npm run check:characters"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "nanostores": "^1.4.2"
  }
}
